apiVersion: v1
kind: Pod
metadata:
  name: bad-privileged
  namespace: mini-platform
  labels:
    app.kubernetes.io/name: bad-privileged
    app.kubernetes.io/owner: training
spec:
  containers:
    - name: shell
      image: busybox:1.36
      command: ["sh", "-c", "sleep 3600"]
      securityContext:
        privileged: true
